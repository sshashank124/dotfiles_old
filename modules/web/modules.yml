firefox:
  requires:
    - installed: firefox

  on_startup:
    import_context:
      from_path: context.yml
      from_section: firefox
      to_section: web

    symlink:
      - content: firefox/web
        target: $HOME/bin/web
