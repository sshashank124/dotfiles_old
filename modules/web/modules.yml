firefox:
  requires:
    - installed: firefox

  import_context:
    from_path: firefox/context.yml


qutebrowser:
  requires:
    - installed: qutebrowser

  import_context:
    from_path: qutebrowser/context.yml
