compton:
  requires:
    - installed: compton

  symlink:
    - content: compton/compton.conf
      target: $XDG_CONFIG_HOME/compton/compton.conf
    - content: compton/daemon_compton
      target: {{ host.user.paths.display_init }}/daemon_compton
