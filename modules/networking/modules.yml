access_point:
  requires:
    - installed: create_ap
    - installed: haveged

  compile:
    - content: access_point/t.access_point
      target: {{ host.user.paths.bin }}/access_point
      permissions: +x
