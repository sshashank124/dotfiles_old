nvidia:
  requires:
    - module: desktop::xorg
    - installed: nvidia-smi

  symlink:
    - content: nvidia/display_source
      target: {{ host.user.paths.bin }}/display_source
    - content: {{ host.user.paths.bin }}/display_source
      target: {{ host.user.paths.display_init }}/10-display_source
