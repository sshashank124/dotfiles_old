state:
  requires:
    - installed: systemctl
    - installed: scrot
    - installed: magick

  compile:
    - content: state/t.lock
      target: $HOME/bin/lock
      permissions: +x

  symlink:
    - content: state/change_state
      target: $HOME/bin/change_state
    - content: state/overlay.png
      target: $XDG_CONFIG_HOME/wallpapers/overlay.png
